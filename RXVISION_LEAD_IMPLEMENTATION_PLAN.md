# RxVision — Σχέδιο υλοποίησης Lead Engine

Συνοδεύει τα υπόλοιπα έξι κείμενα. Εδώ είναι **τι γίνεται, με ποια σειρά, και πώς
επαληθεύεται** ότι δουλεύει.

---

## Αρχές που δεσμεύουν κάθε φάση

1. **Καμία υπάρχουσα λειτουργία δεν αλλάζει συμπεριφορά** χωρίς να γραφτεί εδώ.
2. **Καμία μετανάστευση χωρίς αντίγραφο.** Κανένα `update_many({})` με κενό φίλτρο.
3. **Καμία δοκιμή πάνω στα ζωντανά δεδομένα του ιδιοκτήτη.** Ό,τι δοκιμάζεται, δοκιμάζεται
   σε δικές μου εγγραφές και σβήνεται με ακριβές `_id`.
4. **Καμία αποστολή μέχρι τη Φάση 2**, και καμία μαζική αποστολή χωρίς συγκατάθεση.
5. **Κάθε φάση αφήνει το σύστημα λειτουργικό.** Αν σταματήσουμε στη Φάση 1, το εργαλείο
   είναι ήδη χρήσιμο.

---

## ΦΑΣΗ 1 — Νοημοσύνη leads *(καμία αποστολή)*

**Ο στόχος:** τα 5 φαρμακεία με ληγμένη δοκιμαστική και όσες εγγραφές έμειναν στη μέση να
εμφανιστούν, με πραγματικό σκορ και συγκεκριμένη πρόταση ενέργειας.

### Βήματα

| # | Τι | Αρχείο |
|---|---|---|
| 1.1 | Μετανάστευση `trial_leads` → `leads` (ίδια `_id`, backup) | script |
| 1.2 | Προβολή από 6 πηγές | `services/leads/projection.py` |
| 1.3 | Σήματα & σκορ με ρυθμιζόμενα βάρη | `services/leads/scoring.py` |
| 1.4 | Χρονολόγιο | `services/leads/timeline.py` |
| 1.5 | Σημειώσεις, εργασίες, ετικέτες, ανάθεση, κατάσταση | `services/leads/actions.py` |
| 1.6 | Τμήματα (κανόνες AND/OR, **fail-closed**) | `services/leads/segments.py` |
| 1.7 | Κανόνες «τι κάνω τώρα» | `services/leads/next_action.py` |
| 1.8 | Router με `enforce_section("leads")` | `api/v1/routers/admin_leads.py` |
| 1.9 | Ωριαία προβολή + ημερήσια σκορ | `workers/leads.py` + beat |
| 1.10 | Οθόνη: «Σήμερα», KPI, φίλτρα, πίνακας | `app/admin/leads/page.tsx` |
| 1.11 | Καρτέλα lead με χρονολόγιο | `app/admin/leads/[id]/page.tsx` |
| 1.12 | Μετανάστευση M1 με αντίγραφο + επαλήθευση `afm_had_trial` | script |

### Το σκορ — μόνο μετρήσιμα σήματα

| Σήμα | Βάρος | Πηγή |
|---|---|---|
| Σύνδεσε δεδομένα ΗΔΥΚΑ | +25 | `audit_logs` `PUT /ingestion/credentials/hdika` |
| Ενέργειες 30 ημερών (λογαριθμικά, ανώτατο) | +20 | `audit_logs` |
| Ημέρες με δραστηριότητα 30 ημερών | +15 | `audit_logs` |
| Χρησιμοποίησε ≥3 διαφορετικές περιοχές | +10 | `audit_logs` (κατηγορία, όχι διαδρομή) |
| Ζήτησε δοκιμή/παρουσίαση από pop-up | +15 | `announcement_requests` |
| Ξαναμπήκε μετά από ≥7 ημέρες σιωπής | +10 | σύγκριση προβολών |
| Έφτασε στο ταμείο χωρίς να πληρώσει | +8 | `pending_registrations` |
| Καμία δραστηριότητα >30 ημέρες | −15 | |
| Ποτέ δεν συνδέθηκε | −20 | `users.last_login_at` |
| Ζήτησε διαγραφή / αποκλεισμός | → 0 | |

Βάρη σε `platform_settings._id="leads"`. Κάθε lead κρατά τα σήματά του με τον λόγο, ώστε το
UI να εξηγεί πάντα το νούμερο.

**Τι ΔΕΝ μπαίνει:** ανοίγματα email (δεν υπάρχουν μέχρι τη Φάση 2), προβολές σελίδων (δεν
καταγράφονται). Θα ήταν μηδενικά που μοιάζουν με μέτρηση.

### Επαλήθευση Φάσης 1
- Τα 5 ληγμένα trial εμφανίζονται με σκορ που αντιστοιχεί στις πραγματικές τους ενέργειες
  (Hivepharmacy 69 ενέργειες > ΦΑΡΜΑΚΕΙΟ ΚΑΙΣΑΣ 6 → μεγαλύτερο σκορ).
- Οι 6 παλιοί leads διατηρούν `_id`, `status`, `trial_allowed`.
- `afm_had_trial` συνεχίζει να μπλοκάρει επανα-trial — **έλεγχος πριν και μετά**.
- Staff χωρίς δικαίωμα `leads` παίρνει 403 σε κάθε endpoint.
- Καμία αποστολή: μηδέν νέες γραμμές σε `sent_messages` / `newsletters`.

---

## ΦΑΣΗ 2 — Επικοινωνία

| # | Τι |
|---|---|
| 2.1 | `lead_campaigns` + `lead_recipients` (**μοναδικό κλειδί**) + κύκλος ζωής |
| 2.2 | Worker αποστολής σε παρτίδες, με επαναλήψεις και παύση |
| 2.3 | Τα τέσσερα φρένα, με **λόγο ανά παράλειψη** |
| 2.4 | Διαγραφή: HMAC token + δημόσια σελίδα + 3 επιλογές |
| 2.5 | Πρότυπα (10) με κλειστή λίστα μεταβλητών |
| 2.6 | Ανοίγματα/κλικ |
| 2.7 | Δημιουργία καμπάνιας με **οθόνη έγκρισης** |
| 2.8 | Συγκατάθεση στη φόρμα εγγραφής (μη προ-επιλεγμένη) |
| 2.9 | **Το newsletter γίνεται καμπάνια** — η παλιά διαδρομή αφαιρείται |

**Επαλήθευση:** διπλή υποβολή δεν στέλνει δύο φορές (`DuplicateKeyError`)· πλαστό token
απορρίπτεται· διαγραμμένος εξαιρείται αμέσως· καμία αποστολή δεν τρέχει μέσα σε HTTP request.

⚠️ **Προσοχή στο 2.9:** το `send_newsletter` υπάρχει και δουλεύει. Αφαιρείται **μόνο** αφού η
νέα διαδρομή στείλει επιτυχώς μία πραγματική καμπάνια.

---

## ΦΑΣΗ 3 — Προσφορές

| # | Τι |
|---|---|
| 3.1 | `platform_offers` + 4 appliers |
| 3.2 | `coupons` v2 — **προσθετικά**, με `discount_pct` και `used` να μένουν |
| 3.3 | `coupon_redemptions` |
| 3.4 | Στόχευση μέσω τμημάτων + έκδοση κουπονιού ανά lead |
| 3.5 | Φρένα: ανώτατο ποσοστό, ένα ενεργό ανά lead, υποχρεωτική λήξη |
| 3.6 | Απόδοση μετατροπής (last-touch, με αποθηκευμένες όλες τις επαφές) |

**Επαλήθευση:** παλιό κουπόνι (η 1 υπάρχουσα εγγραφή) εξαργυρώνεται **ακριβώς** όπως πριν·
νέο `fixed`/`free_period` δίνει σωστό ποσό/περίοδο· ποτέ κάτω από €0,30.

---

## ΦΑΣΗ 4 — Αυτοματισμοί

`lead_journeys` + `lead_journey_runs` με μοναδικό `{journey_id, lead_key}`, βήματα
`wait|send|task|tag|branch`, ημερήσιο όριο, **όλα τα φρένα της Φάσης 2 σε κάθε βήμα
αποστολής**. Κάθε διαδρομή ξεκινά **ανενεργή** και θέλει ρητή ενεργοποίηση.

---

## ΦΑΣΗ 5 — AI

Προτάσεις θέματος/κειμένου/προσφοράς/χρονισμού από το πλαίσιο του lead. Επιστρέφει πάντα
`needs_review: true`. **Καμία αυτόματη αποστολή** — ίδιος κανόνας με το AI των ασθενών.

---

## Νέες εργασίες χρονοπρογράμματος

| Εργασία | Πότε | Τι |
|---|---|---|
| `leads.project` | κάθε ώρα | ανανέωση προβολής + μεταβάσεις + γεγονότα |
| `leads.score` | 06:00 UTC | επανυπολογισμός σκορ |
| `leads.digest` | 06:30 UTC | ημερήσιο email «τι χρειάζεται προσοχή» (**μόνο** αν έχει περιεχόμενο) |
| `leads.dispatch` | κάθε 5′ | ουρά καμπανιών *(Φάση 2)* |
| `leads.journeys` | κάθε 15′ | προώθηση διαδρομών *(Φάση 4)* |

---

## Τι δεν αγγίζεται

`campaign_engine`, `comms`, `audience`, `automations` (ασθενείς) · η διαδρομή πληρωμής ·
`purge_expired_trials` · `afm_had_trial` · `subscription_reminders` · `announcements` ·
οποιοδήποτε tenant endpoint.

---

## Κίνδυνοι

| Κίνδυνος | Αντίμετρο |
|---|---|
| Η προβολή γράφει λάθος και χαλάει τα 6 leads | αντίγραφο πριν· η προβολή γράφει **μόνο** cache πεδία, ποτέ `status`/`trial_allowed` |
| Σπάσιμο του μπλοκ επανα-trial | `afm_had_trial` αμετάβλητη· έλεγχος πριν/μετά |
| Σπάσιμο εξαργύρωσης κουπονιού | μόνο προσθετικά πεδία· τα παλιά διαβάζονται όπως πριν |
| Μαζική αποστολή κατά λάθος | Φάση 1 = μηδέν αποστολή· Φάση 2 με έγκριση και τέσσερα φρένα |
| Ο `admin.py` μεγαλώνει άλλο | ξεχωριστός router |
| Το σκορ φαίνεται αυθαίρετο | κάθε σήμα με τον λόγο του, ορατό στο UI |

---

## Σειρά παράδοσης

Φάση 1 πρώτα και ολόκληρη — γιατί λύνει το πραγματικό πρόβλημα (5 θερμά φαρμακεία αόρατα)
χωρίς να στείλει ούτε ένα email, άρα χωρίς κανένα νομικό ή φήμης ρίσκο. Οι επόμενες
χτίζονται πάνω σε κάτι που ήδη χρησιμοποιείται.
