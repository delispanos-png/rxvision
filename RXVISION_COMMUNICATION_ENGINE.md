# RxVision — Communication Engine: αρχιτεκτονική & σχέδιο

> Ημ/νία: **17/09/2026** · Έκδοση: v1.39.x
> **Καμία γραμμή κώδικα δεν γράφτηκε.** Αυτό είναι σχέδιο προς έγκριση (§37).

---

## 0. Το συμπέρασμα πρώτα

**Υπάρχει ήδη περίπου το 45% αυτού που ζητάς — και δουλεύει.** Κανάλια, χρέωση, συγκατάθεση,
τμηματοποίηση, κουπόνια με ROI: όλα υλοποιημένα και σε παραγωγή.

Αυτό που λείπει δεν είναι «σύστημα αποστολής». Είναι **τα πάντα γύρω από την αποστολή**:
πρότυπα, χρονοπρογραμματισμός, αυτοματισμοί, διαγραφή από λίστα, μετρήσεις ανοίγματος.

Και υπάρχει **ένα σοβαρό πρόβλημα παραγωγής** που πρέπει να διορθωθεί πριν χτιστεί οτιδήποτε
από πάνω → **R-1**.

---

## 1. Τρέχουσα αρχιτεκτονική

```
app.rxvision.gr /communications
        │
        ▼
routers/communications.py ──────► services/comms.py (39 KB)
        │                              │
        │                              ├── Apifon: SMS · Viber (OAuth, DLR webhook)
        │                              ├── mailer.py: κεντρικό SMTP
        │                              ├── push_service.py: PWA Web Push (ΔΩΡΕΑΝ)
        │                              ├── message_wallets: προπληρωμένο πορτοφόλι
        │                              ├── message_ledger: κάθε χρέωση
        │                              └── sent_messages: κάθε μήνυμα + DLR + επιστροφή
        │
        └──► services/marketing.py ──► campaign_coupons → ROI ανά καμπάνια
```

## 2. Υπάρχοντα στοιχεία που ΞΑΝΑΧΡΗΣΙΜΟΠΟΙΟΥΝΤΑΙ

| Στοιχείο | Πού | Κατάσταση |
|---|---|---|
| Αποστολή Email / SMS / Viber / Push | `comms.py` | ✅ Παραγωγή |
| Πορτοφόλι & χρέωση ανά μήνυμα | `message_wallets`, `message_ledger` | ✅ Με αυτόματη ανανέωση |
| DLR & επιστροφή χρημάτων σε αποτυχία | `/apifon-dlr` webhook | ✅ |
| Αναγνωριστικό αποστολέα ανά φαρμακείο | `tenant_comms` + έγκριση admin | ✅ |
| Έλεγχος συγκατάθεσης | `marketing_consent` + `notify_prefs` | ✅ Διπλό επίπεδο |
| Όριο συχνότητας | `_frequency_cap()` | ✅ Υπάρχει |
| Τμηματοποίηση | `segment_patient_ids()` | ⚠️ 5 μόνο segments |
| Απόδοση καμπάνιας (κουπόνια) | `marketing.campaigns_with_roi` | ✅ Πραγματικό attribution |
| Ουρά εργασιών | Celery + Redis | ✅ Υπάρχει, **δεν χρησιμοποιείται εδώ** |
| Ημερολόγιο ελέγχου | `audit_logs` + middleware | ✅ |
| Απομόνωση φαρμακείων | `BaseRepository` | ✅ Εξ ορισμού |

## 3. Υπάρχον μοντέλο συγκατάθεσης — **δύο ανεξάρτητα επίπεδα**

| Επίπεδο | Πού | Ποιος το ορίζει | Τι καλύπτει |
|---|---|---|---|
| **1. Εμπορική συγκατάθεση** | `patient_contacts.marketing_consent` | Ο φαρμακοποιός (στην καρτέλα) | Επιτρέπεται καθόλου προωθητικό; |
| **2. Προτίμηση καναλιού** | `patient_accounts.notify_prefs{sms,viber,email,push}` | **Ο ίδιος ο ασθενής**, από την πύλη | Σε ποιο κανάλι |

Συν `ESSENTIAL_KINDS` (παραγγελία, ραντεβού, τιμολόγιο…) που **παρακάμπτουν** τις προτιμήσεις:
ο διαχωρισμός λειτουργικού/προωθητικού (§18) **υπάρχει ήδη**.

**Κενό:** δεν υπάρχει `unsubscribed` σε επίπεδο ασθενή — μόνο ανά κανάλι.

## 4. Υπάρχοντα δεδομένα πελατών

`patients_anonymized` (ψευδώνυμο ανά φαρμακείο, `rx_count`, `rx_value_total`, `last_seen_at`,
`age_group`, `lifecycle`) · `patient_contacts` (κινητό/email/συγκατάθεση/ενεργός) ·
`patient_accounts` (λογαριασμός πύλης + προτιμήσεις) · `loyalty_members` (πόντοι/βαθμίδα) ·
`prescription_executions` · `orders_delivery`.

**Όλα όσα χρειάζεται ο Audience Builder υπάρχουν ήδη.**

---

## 5. Τι ΛΕΙΠΕΙ — επαληθευμένο με αναζήτηση στον κώδικα

| Ζητούμενο | Ευρήματα |
|---|---|
| Χρονοπρογραμματισμός (`scheduled_at`) | **0** |
| Πρότυπα μηνυμάτων | **0** |
| Αυτοματισμοί / triggers | **0** |
| Διαγραφή από λίστα (unsubscribe) | **0** |
| Ανοίγματα / κλικ | **0** |
| Προσχέδια, παύση, ακύρωση | **0** |
| Δοκιμαστική αποστολή | ⚠️ Μόνο `/test-email` χωρίς πλαίσιο καμπάνιας |

---

## 6. ⚠️ R-1 — ΤΟ ΚΡΙΣΙΜΟ: η αποστολή τρέχει ΜΕΣΑ στο HTTP request

`routers/communications.py:196` → `await comms.run_campaign(...)` — βρόχος **έως 2.000
παραληπτών** με κλήση δικτύου ανά παραλήπτη, μέσα στο αίτημα. Επιστρέφει `202` αλλά **δεν έχει
γίνει τίποτα ασύγχρονα**.

**Συνέπειες σήμερα:**

| | |
|---|---|
| Χρονικό όριο | 2.000 × ~300 ms ≈ **10 λεπτά** — ο proxy κόβει πολύ νωρίτερα |
| Μερική αποστολή | Ο φαρμακοποιός δεν μαθαίνει ποτέ πού σταμάτησε |
| Καμία επανάληψη | Μία αποτυχία παρόχου = χαμένος παραλήπτης, σιωπηλά |
| Καμία ιδιοπάθεια | Refresh/διπλό κλικ = **διπλή αποστολή και διπλή χρέωση** |
| Αόρατη πρόοδος | Καμία ένδειξη «2.000 από 2.000» |

**Άμεση διόρθωση (Φάση 1):** η `/send` γράφει καμπάνια σε κατάσταση `queued` και επιστρέφει·
Celery task στέλνει σε παρτίδες με ρυθμό, επαναλήψεις και κλειδί ιδιοπάθειας ανά
`(campaign_id, patient_ref)`.

**Αυτό πρέπει να γίνει πριν προστεθεί οτιδήποτε άλλο.** Ό,τι χτιστεί από πάνω κληρονομεί το λάθος.

---

## 7. Προτεινόμενη αρχιτεκτονική

Επεκτείνει το υπάρχον. **Καμία παράλληλη υποδομή** (§2, §29).

```
Communication Engine
│
├── Audience Engine      services/audience.py         ΝΕΟ  — κανόνες → patient_refs
├── Campaign Engine      services/comms.py            ΕΠΕΚΤΑΣΗ — καταστάσεις, παρτίδες
├── Template Engine      services/msg_templates.py    ΝΕΟ  — μεταβλητές + εφεδρικές τιμές
├── Automation Engine    services/automations.py      ΝΕΟ  — trigger → conditions → βήματα
├── Consent Engine       notify_prefs.py + contacts   ΥΠΑΡΧΕΙ — + unsubscribe
├── Scheduler            workers/comms.py             ΝΕΟ  — Celery beat
├── Provider Layer       comms.py                     ΑΝΑΔΙΑΤΑΞΗ σε πρωτόκολλα
└── Analytics            marketing.py                 ΕΠΕΚΤΑΣΗ — ανοίγματα/κλικ
```

### Επίπεδο παρόχων (§9)

Σήμερα ο Apifon είναι μπλεγμένος μέσα στη `comms.py`. Πρόταση: `typing.Protocol` ανά κανάλι
(`EmailProvider`, `SmsProvider`, `ViberProvider`, `PushProvider`), ο Apifon γίνεται μία
υλοποίηση. **Χωρίς αλλαγή συμπεριφοράς** — μόνο αναδιάταξη.

## 8. Αλλαγές βάσης

**Νέες συλλογές** (όλες tenant-scoped μέσω `BaseRepository`):

| Συλλογή | Ρόλος |
|---|---|
| `comm_audiences` | Αποθηκευμένο κοινό: κανόνες, όχι στιγμιότυπο |
| `comm_templates` | Πρότυπα ανά κατηγορία + αντίγραφα φαρμακείου |
| `comm_recipients` | Ένας παραλήπτης ανά καμπάνια: κατάσταση, προσπάθειες, κλειδί ιδιοπάθειας |
| `comm_automations` | trigger + conditions + βήματα |
| `comm_automation_runs` | Εκτελέσεις — σε ποιο βήμα βρίσκεται ποιος |
| `comm_events` | delivered / opened / clicked / bounced / unsubscribed |

**Επεκτάσεις υπαρχόντων** (χωρίς breaking change):

- `comms_campaigns` → `status`, `scheduled_at`, `audience_id`, `template_id`, `created_by`,
  `approved_by`, `counts{}`, `recurrence`
- `patient_contacts` → `unsubscribed_at`, `unsubscribe_token`
- `sent_messages` → `campaign_id` (υπάρχει), `opened_at`, `clicked_at`

**Δεν δημιουργείται** `CommunicationConsent`/`CommunicationPreference` — **υπάρχουν ήδη** ως
`marketing_consent` + `notify_prefs` (§31: όχι διπλές οντότητες).

## 9. Αλλαγές API

`GET/POST/PUT/DELETE /communications/audiences` · `.../audiences/{id}/preview` (πλήθος +
εξαιρέσεις με αιτιολογία) · `/templates` · `/campaigns` (CRUD + `/approve`, `/pause`, `/cancel`,
`/test-send`) · `/automations` · `/calendar` · `/analytics/{id}` ·
**δημόσια** `GET /u/{token}` (unsubscribe χωρίς σύνδεση) · `GET /o/{id}.gif`, `GET /c/{id}`
(άνοιγμα/κλικ).

## 10. Μοντέλο ασφαλείας

Απομόνωση **εξ ορισμού** μέσω `BaseRepository`. Δικαιώματα: `portal:manage` για σύνταξη,
**νέο `comms:approve`** για την τελική αποστολή — ο διαχωρισμός σύνταξης/έγκρισης είναι η
ουσιαστική δικλείδα. Καταγραφή σε `audit_logs`: created/edited/approved/scheduled/sent/cancelled.
Τα δημόσια tokens (unsubscribe/pixel) είναι HMAC με per-tenant pepper, χωρίς ΑΜΚΑ.

## 11. GDPR & δεδομένα υγείας — §19

**Ο κίνδυνος:** τα σημερινά segments `icd`, `therapy`, `substance` τμηματοποιούν με **δεδομένα
υγείας**. Είναι θεμιτά για **φροντίδα** (υπενθύμιση επανάληψης), **όχι** για προώθηση
(«επειδή παίρνετε φάρμακα για…»).

**Πρόταση — τεχνική δικλείδα, όχι σύσταση:**

```
purpose = "care" | "commercial"
```

Κάθε καμπάνια δηλώνει σκοπό. Τα κλινικά segments είναι **διαθέσιμα μόνο με `purpose="care"`**,
και τέτοια καμπάνια **δεν επιτρέπεται** να περιέχει κουπόνι ή προσφορά. Το σύστημα το εμποδίζει
τεχνικά — δεν αρκεί προειδοποίηση.

Επιπλέον: το κείμενο **ποτέ** δεν αναφέρει πάθηση ή φάρμακο· λέει «η επανάληψη της αγωγής σου».
Οι μεταβλητές δεν εκθέτουν ποτέ κλινικά δεδομένα.

---

## 12. Φάσεις

| | Περιεχόμενο | Γιατί με αυτή τη σειρά |
|---|---|---|
| **Φάση 0** | **R-1**: αποστολή σε ουρά + ιδιοπάθεια + επαναλήψεις + πρόοδος | Θεμέλιο. Χωρίς αυτό, ό,τι χτιστεί κληρονομεί το σφάλμα |
| **Φάση 1** | Καταστάσεις καμπάνιας · προσχέδια · δοκιμαστική αποστολή · οθόνη έγκρισης με πλήθος & εξαιρέσεις · **unsubscribe** · πρότυπα | Το unsubscribe είναι **νομική υποχρέωση** και λείπει |
| **Φάση 2** | Audience Builder · έτοιμα segments · αποθηκευμένα κοινά · `purpose` δικλείδα | Η μεγάλη αξία για τον φαρμακοποιό |
| **Φάση 3** | Χρονοπρογραμματισμός · επαναλαμβανόμενες · ημερολόγιο · αυτοματισμοί | Χρειάζεται σταθερή Φάση 0 |
| **Φάση 4** | Ανοίγματα/κλικ · attribution · AI βοηθός σύνταξης (**πάντα με ανθρώπινη έγκριση**) | Βελτιστοποίηση |

**Πρόταση:** Φάση 0 **τώρα** και ανεξάρτητα — είναι διόρθωση, όχι χαρακτηριστικό.

## 13. Κίνδυνοι

| | Κίνδυνος | Μετριασμός |
|---|---|---|
| **R-1** | Αποστολή μέσα στο request → διπλές χρεώσεις, μερικές αποστολές | Φάση 0 |
| **R-2** | Τμηματοποίηση με δεδομένα υγείας για προώθηση | Δικλείδα `purpose` |
| **R-3** | Λείπει unsubscribe σε προωθητικά email | Φάση 1 |
| **R-4** | Το όριο συχνότητας υπάρχει αλλά **δεν φαίνεται** στον φαρμακοποιό | Στην οθόνη έγκρισης |
| **R-5** | Εξάντληση πορτοφολιού στη μέση | Έλεγχος πριν την ουρά + παύση, όχι σιωπηλή διακοπή |
| **R-6** | Το εργαλείο γίνεται μηχανή spam (§34) | Όριο ορατό · ημερολόγιο · «τελευταία επικοινωνία» στην καρτέλα |

## 14. Ανοιχτά ερωτήματα

1. **Έγκριση:** χωριστό δικαίωμα `comms:approve` ή αρκεί ο ιδιοκτήτης;
2. **Ανοίγματα email:** το pixel είναι παρακολούθηση. Το ενεργοποιούμε; Προτείνω **προαιρετικά
   ανά φαρμακείο, κλειστό εξ ορισμού**.
3. **Όριο συχνότητας:** καθολικό από adminpanel ή ανά φαρμακείο; Σήμερα ανά φαρμακείο.
4. **Ποιος πληρώνει τα προωθητικά;** Σήμερα το πορτοφόλι του φαρμακείου. Παραμένει;
5. **Κλινικά segments:** συμφωνείς με τη δικλείδα `purpose`, ή τα κλειδώνουμε τελείως;
6. **Attribution:** μόνο μέσω κουπονιού (όπως σήμερα) ή και «αγόρασε εντός 14 ημερών»;
