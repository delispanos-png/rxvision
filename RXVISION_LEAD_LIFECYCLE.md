# RxVision — Κύκλος ζωής ενός lead

Συνοδεύει το `RXVISION_LEAD_ENGINE_ARCHITECTURE.md`.

---

## 1. Δύο άξονες, όχι ένας

Το πρώτο λάθος που αποφεύγουμε: να βάλουμε σε **μία** στήλη «κατάσταση» δύο πράγματα που
αλλάζουν ανεξάρτητα.

- **Πού βρίσκεται ο λογαριασμός του** (το ξέρει το σύστημα, κανείς δεν το γράφει με το χέρι):
  δοκιμάζει, λήγει, έληξε, διαγράφηκε, πληρώνει.
- **Πού βρίσκεται η κουβέντα μας μαζί του** (το γράφει ο πωλητής): δεν τον έχουμε αγγίξει,
  του μιλήσαμε, μας απάντησε, του στείλαμε προσφορά, διαπραγματευόμαστε, τελείωσε.

Αν τα ενώσεις, χάνεις πληροφορία: «CONTACTED» δεν σου λέει αν το trial του τρέχει ή έληξε πριν
τρεις μήνες — και αυτό αλλάζει εντελώς το τι θα του πεις.

Άρα κάθε lead έχει **δύο** πεδία: `stage` (αυτόματο) και `status` (χειροκίνητο).

---

## 2. `stage` — η κατάσταση του λογαριασμού (αυτόματη)

Υπολογίζεται αποκλειστικά από `billing_service.effective_status` + ημερομηνίες. **Κανείς δεν
τη θέτει με το χέρι** — αλλιώς θα έλεγε ψέματα μέσα σε μια μέρα.

| stage | Ελληνικά στο UI | Συνθήκη |
|---|---|---|
| `signup_abandoned` | Δεν ολοκλήρωσε την εγγραφή | `pending_registrations` >24ω, χωρίς πληρωμή |
| `trial_active` | Δοκιμάζει | trial, λήξη >7 ημέρες |
| `trial_ending` | Η δοκιμή τελειώνει | trial, λήξη ≤7 ημέρες |
| `trial_expired` | Η δοκιμή έληξε | trial, λήξη πέρασε, λογαριασμός ζωντανός |
| `trial_purged` | Ο λογαριασμός διαγράφηκε | από `purge_expired_trials` |
| `churned` | Ήταν πελάτης, έφυγε | πληρωμένη συνδρομή που έληξε |
| `customer` | Πελάτης | ενεργή πληρωμένη συνδρομή |

Ο πίνακας μεταβαίνει μόνος του. Η μετάβαση γράφεται στο χρονολόγιο (`trial.expiring`,
`trial.expired`, `lead.converted` …) ώστε να φαίνεται **πότε** άλλαξε.

### Χρονικά παράθυρα (ρυθμιζόμενα)

`platform_settings._id = "leads"`:

```
trial_ending_days: 7          # πότε λέμε «τελειώνει»
expired_buckets:  [7, 30, 60, 90]
signup_abandoned_hours: 24
```

Τα «καλάθια» λήξης παράγουν τα έτοιμα φίλτρα: έληξε 1–7 / 8–30 / 31–60 / 61–90 / 90+ ημέρες
πριν. Αλλάζοντας τη λίστα αλλάζουν και τα φίλτρα — δεν είναι γραμμένα στον κώδικα.

---

## 3. `status` — πού είναι η κουβέντα (χειροκίνητο)

| status | Ελληνικά στο UI | Ποιος το βάζει |
|---|---|---|
| `new` | Δεν του έχουμε μιλήσει | αυτόματα με τη δημιουργία |
| `contacted` | Του μιλήσαμε | πωλητής, ή αυτόματα με την 1η αποστολή |
| `engaged` | Ανταποκρίθηκε | πωλητής, ή αυτόματα σε άνοιγμα/κλικ/επιστροφή |
| `offer_sent` | Πήρε προσφορά | αυτόματα με την αποστολή προσφοράς |
| `negotiating` | Το συζητάμε | πωλητής |
| `won` | Έγινε πελάτης | αυτόματα με την ενεργοποίηση συνδρομής |
| `lost` | Δεν προχωράει | πωλητής, με **υποχρεωτικό λόγο** |
| `do_not_contact` | Δεν θέλει επικοινωνία | πωλητής ή διαγραφή από τον ίδιο |

### Κανόνες μετάβασης

- Ο πωλητής μπορεί να πάει **οπουδήποτε** εκτός από `won` (το δίνει μόνο η πληρωμή) και
  `do_not_contact` από αυτόματη διαδικασία (μόνο άνθρωπος ή ο ίδιος ο παραλήπτης).
- `do_not_contact` και `lost` είναι **απορροφητικά για τα αυτόματα**: καμία καμπάνια, καμία
  διαδρομή, κανένα αυτόματο μήνυμα. Ο πωλητής μπορεί ακόμη να σηκώσει τηλέφωνο — αυτό είναι
  απόφαση ανθρώπου και καταγράφεται.
- Κάθε αλλαγή γράφει γραμμή `lead_status_history`: **από**, **σε**, **ποιος**, **πότε**,
  **γιατί**. Δεν διαγράφεται ποτέ.

---

## 4. Τι γίνεται όταν ένας lead γίνει πελάτης

Ο κανόνας είναι: **ο lead δεν σβήνεται και δεν μετακινείται.**

```
status = won · stage = customer
conversion = { πότε, πακέτο, ποσό, καμπάνια, προσφορά, κουπόνι, κανάλι, πωλητής }
```

Ο `lead_key` παραμένει ο ίδιος, άρα όλο το χρονολόγιο —πρώτη δοκιμή, κάθε email, κάθε
τηλέφωνο, η προσφορά που έπιασε— μένει διαθέσιμο. Αυτό είναι που κάνει δυνατά τα
μελλοντικά upsell/renewal/win-back: ξέρουμε **πώς** τον κερδίσαμε.

Αν αργότερα φύγει, ο ίδιος lead γίνεται `stage=churned`, `status=new` — και το ιστορικό του
τον περιμένει.

---

## 5. Χρονολόγιο — μία γραμμή, όλες οι πηγές

Το `lead_events` είναι η καρδιά. Κάθε γραμμή: `lead_key`, `kind`, `at`, `by`, `data`.

Πραγματικό παράδειγμα με τη σημερινή δομή δεδομένων:

```
14/08  trial.started      δοκιμαστική 20 ημερών
15/08  lead.activity      σύνδεσε δεδομένα ΗΔΥΚΑ            ← ισχυρότατο σήμα
15/08  lead.activity      6 ενέργειες (σαρώσεις συνταγών)
21/08  lead.activity      23 ενέργειες
03/09  trial.expiring     λήγει σε 1 ημέρα
04/09  trial.expired      η δοκιμή έληξε
04/09  lead.created       μπήκε στη λίστα πωλήσεων, σκορ 62
06/09  email.sent         «Η δοκιμή σου τελείωσε»
06/09  email.opened
10/09  lead.returned      ξαναμπήκε μετά από 6 ημέρες        ← σήμα επιστροφής
10/09  lead.note          «Θέλει πρώτα να δει το Loyalty»
11/09  lead.task_created  τηλέφωνο μετά τις 15:00 — Π.Δ.
```

Πριν σηκώσει τηλέφωνο, ο πωλητής διαβάζει **αυτό** — όχι δέκα καρτέλες.

---

## 6. Γιατί όχι τα statuses της αρχικής πρότασης

Ζητήθηκαν ενδεικτικά: `NEW, TRIAL_ACTIVE, TRIAL_ENDING, TRIAL_EXPIRED, CONTACTED, ENGAGED,
OFFER_SENT, FOLLOW_UP, RE_ENGAGEMENT, NEGOTIATION, CONVERTED, LOST, DO_NOT_CONTACT` — με τη
ρητή σημείωση να προτείνω καλύτερη δομή αν υπάρχει.

Τι κράτησα και τι άλλαξα:

- **Έσπασα σε δύο άξονες** (§1). Τα `TRIAL_*` είναι κατάσταση λογαριασμού, τα `CONTACTED`,
  `ENGAGED`, `NEGOTIATION` είναι κατάσταση κουβέντας. Ένα φαρμακείο είναι ταυτόχρονα «η
  δοκιμή του έληξε» **και** «του στείλαμε προσφορά». Με μία στήλη θα έπρεπε να διαλέξεις.
- **`FOLLOW_UP` δεν είναι κατάσταση, είναι εργασία.** Γίνεται `lead_tasks` με ημερομηνία,
  υπεύθυνο και προτεραιότητα — αλλιώς δεν ξέρεις *πότε* και *ποιος*.
- **`RE_ENGAGEMENT` δεν είναι κατάσταση, είναι τμήμα.** Προκύπτει από κανόνα (έληξε >60
  ημέρες, χωρίς επαφή 30 ημέρες, όχι do-not-contact). Αν ήταν στήλη, θα έπρεπε κάποιος να τη
  συντηρεί με το χέρι — και θα άρχιζε να λέει ψέματα την πρώτη εβδομάδα.
- **Πρόσθεσα `signup_abandoned` και `churned`**, γιατί υπάρχουν πραγματικά στα δεδομένα: 15
  ημιτελείς εγγραφές σήμερα, και πελάτες που θα λήξουν αύριο.
