# RxVision — Lead Lifecycle & Conversion Engine
## Αρχιτεκτονική (μετά από πλήρες audit του υπάρχοντος κώδικα)

**Ημερομηνία:** 17/09/2026 · **Κλάδος:** `feature/loyalty-portal-build`
**Κατάσταση:** σχεδιασμός — καμία αλλαγή κώδικα δεν έχει γίνει όταν γράφτηκε αυτό το κείμενο.

---

## 0. Το εύρημα που αλλάζει τα πάντα

Πριν τη σχεδίαση μέτρησα τι υπάρχει **στην παραγωγή**, όχι τι λέει ο κώδικας.

| | |
|---|---|
| Φαρμακεία (tenants) | **12** |
| Με **ληγμένη δοκιμαστική αλλά ζωντανό λογαριασμό** | **5** |
| Εγγραφές στη σελίδα «Leads» | **6** |
| Από αυτές **χωρίς email** (ανέφικτη επαφή) | **3** |
| Προσφορές που έχουν σταλεί ποτέ (`offers_sent`) | **0** |
| Εγγραφές «πληρωμή-πρώτα» που έμειναν στη μέση | **4** (από 15· οι 11 ολοκληρώθηκαν) |
| Γραμμές `audit_logs` (06/06 → 17/09) | **18.207** |

**Η σημερινή σελίδα «Leads — πρώην δοκιμαστικοί» δείχνει ακριβώς τα φαρμακεία που δεν
μπορούμε πια να βοηθήσουμε, και κρύβει αυτά που μπορούμε.**

Ο λόγος είναι δομικός: `trial_leads` γεμίζει **μόνο** από την `purge_expired_trials`, δηλαδή
20+ ημέρες **μετά** τη λήξη, τη στιγμή που ο λογαριασμός διαγράφεται. Μέχρι τότε το φαρμακείο
δεν είναι lead πουθενά. Όταν γίνει lead, έχει ήδη χαθεί ο λογαριασμός του, η δραστηριότητά του
και συχνά το email του (3 στα 6).

Τα 5 φαρμακεία με ληγμένη δοκιμαστική **αυτή τη στιγμή** — ΦΕΪΖΟΥΛΑ, THE CLINICAL PHARMACY,
ΦΑΡΜΑΚΕΙΟ ΚΑΙΣΑΣ, Hivepharmacy, ΣΥΝΕΡΓΑΖΟΜΕΝΑ ΛΑΡΙΣΑΣ — έχουν ζωντανά δεδομένα, μετρήσιμη
χρήση και ανοιχτό λογαριασμό. Είναι τα θερμότερα leads της επιχείρησης και **δεν εμφανίζονται
σε καμία οθόνη πωλήσεων**.

Επίσης: **4 εγγραφές έμειναν στο `awaiting_payment`** — άνθρωποι που έφτασαν μέχρι το
ταμείο και δεν πλήρωσαν. Δεν εμφανίζονται πουθενά. (Οι 3 από τις 4 είναι επαναλαμβανόμενες
προσπάθειες του ίδιου φαρμακείου που τελικά έγινε πελάτης, και η 4η είναι η ίδια η CloudOn —
γι' αυτό η προβολή ενώνει με ΑΦΜ και εξαιρεί τα δικά μας.)

> **Συμπέρασμα σχεδίασης:** το Lead Engine δεν είναι νέα σελίδα πάνω από τον πίνακα
> `trial_leads`. Είναι **αλλαγή του ορισμού του lead**: lead = κάθε φαρμακείο που έδειξε
> πρόθεση και δεν έχει ενεργή πληρωμένη συνδρομή — ανεξάρτητα από το αν ο λογαριασμός του
> ζει, έληξε ή διαγράφηκε.

---

## 1. Τι υπάρχει ήδη (audit)

### 1.1 Μοντέλα δεδομένων

| Collection | Πεδίο | Τι κρατά | Σχέση με leads |
|---|---|---|---|
| `tenants` | platform | φαρμακείο, `status` (active/trial), `billing_profile`, `company` | **ο lead όσο ζει ο λογαριασμός** |
| `subscriptions` | platform | `status`, `plan`, `trial_ends_at`, `current_period_end`, `seats` | **η αλήθεια για trial/λήξη** |
| `users` | platform | `email`, `full_name`, `last_login_at`, `role_ids` | **επαφή + τελευταία σύνδεση** |
| `trial_leads` | platform | αρχείο ΑΦΜ μετά τη διαγραφή· `status` lead/contacted/converted/unsubscribed | ο lead **αφού** χαθεί |
| `pending_registrations` | platform | ημιτελής «πληρωμή-πρώτα» εγγραφή | **lead πριν καν το trial** |
| `audit_logs` | platform | κάθε POST/PUT/PATCH/DELETE + login, με `tenant_id` & `at` | **η μόνη μόνιμη πηγή συμπεριφοράς** |
| `user_sessions` | platform | ζωντανές συνεδρίες, **TTL 15′** | στιγμιαίο· άχρηστο ως ιστορικό |
| `coupons` | platform | `discount_pct`, `tenant_id`, μίας χρήσης | βάση για προσφορές |
| `feedback` | platform | ερωτηματολόγιο churn + coupon | σήμα πρόθεσης |
| `newsletters` | platform | ιστορικό μαζικού email | το σημερινό «campaign» |
| `announcement_requests` | platform | «θέλω να το δοκιμάσω» / «δείξτε μου το» | **σήμα αγοραστικής πρόθεσης** |

### 1.2 Επικοινωνία — δύο ξεχωριστά συστήματα, όχι ένα

Αυτό είναι κρίσιμο για τον κανόνα «μη φτιάξεις δεύτερο σύστημα επικοινωνίας»:

**Α. Επικοινωνία φαρμακείου → ασθενή** (`comms.py`, `campaign_engine.py`, `audience.py`,
`automations.py`, `unsubscribe.py`, `comm_analytics.py` — Φάσεις 0–4, ολοκληρωμένες 17/09).
Ώριμο: ουρά αποστολής εκτός HTTP, μοναδικό κλειδί παραλήπτη ανά καμπάνια (καμία διπλή
χρέωση), μητρώο συγκατάθεσης, διαγραφή με HMAC token, όριο συχνότητας, φρουρός κλινικών
πεδίων. **Χρεώνει το πορτοφόλι του φαρμακείου** και στοχεύει `patient_contacts`.

**Β. Επικοινωνία πλατφόρμας → φαρμακείο** (`mailer.py`, `admin.py::send_newsletter`,
`billing_service.subscription_reminders`, `feedback_service.send_feedback_emails`,
`comms.admin_alert`). Ανώριμο: το newsletter στέλνει **σύγχρονα μέσα στο HTTP request**
(ακριβώς το R-1 που διορθώσαμε στο Α), χωρίς συγκατάθεση, χωρίς διαγραφή, χωρίς μετρήσεις,
χωρίς όριο συχνότητας, χωρίς επανάληψη σε αποτυχία.

> **Απόφαση:** το Lead Engine **δεν φτιάχνει τρίτο σύστημα**. Γίνεται το **Β**, σωστά — και
> απορροφά το newsletter. Το Α δεν το αγγίζουμε: διαφορετικό υποκείμενο (ασθενής vs
> φαρμακείο), διαφορετικός αποστολέας (πορτοφόλι φαρμακείου vs κεντρικό SMTP), διαφορετική
> νομική βάση (δεδομένα υγείας vs B2B επαφή). Η κοινή μηχανική (ουρά, μοναδικό κλειδί,
> παρτίδες, παύση) **αντιγράφεται ως σχέδιο, όχι ως κώδικας** — και τεκμηριώνεται γιατί.

### 1.3 Ασφάλεια & πρόσβαση

- Δύο κρυπτογραφικά ξεχωριστές ταυτότητες: tenant (`tid`) και platform admin (`padmin`),
  με διαφορετικό `JWT_SECRET` και `aud`.
- `enforce_section` = έλεγχος ανά ενότητα sidebar, **fail-closed** σε άγνωστο segment για
  κάθε POST/PUT/PATCH/DELETE. Η ενότητα `leads` υπάρχει ήδη.
- Τα endpoints `/admin/leads*` δηλώνουν `get_platform_admin`, αλλά ο router φέρει
  `dependencies=[Depends(enforce_section)]` σε επίπεδο router — άρα ο έλεγχος ενότητας
  **ισχύει ήδη**. Ο νέος router κρατά την ίδια εγγύηση ρητά, ανά διαδρομή.
- `AuditMiddleware` γράφει κάθε μεταβολή, με `actor_kind=platform_admin`.

### 1.4 Χρονοπρογραμματισμός

Celery beat, ~25 εργασίες. Σχετικές: `bill_subscriptions`, `subscription_reminders`
(υπενθυμίσεις λήξης — **λειτουργική** επικοινωνία, όχι marketing), `trial_feedback`,
`purge_expired_trials` (05:45 UTC), `sweep_campaigns` (κάθε 5′), `run_automations` (06:15 UTC).

**Δεν υπάρχει event bus.** Καμία `emit`/`publish`. Όλα είναι είτε άμεση κλήση είτε cron.

---

## 2. Τι μπορούμε πραγματικά να μετρήσουμε (και τι όχι)

Ο κανόνας «καμία ψεύτικη μέτρηση» σημαίνει ότι πρέπει να πω τι **δεν** βλέπω.

**Βλέπω σήμερα:**
- Ημερομηνίες trial, λήξης, υπόλοιπες/περασμένες ημέρες → `subscriptions`.
- Τελευταία σύνδεση ανά χρήστη → `users.last_login_at`.
- Κάθε **ενέργεια** (POST/PUT/PATCH/DELETE) με `tenant_id` + ώρα → `audit_logs`.
  Παράδειγμα πραγματικών δεδομένων 90 ημερών: 12.594 σαρώσεις συνταγών, 208 ερωτήσεις
  PharmaCat, 151 Copilot, 92 συμβουλές Patient Intelligence, 26 ρυθμίσεις Loyalty.
- Σύνδεση δεδομένων ΗΔΥΚΑ (`PUT /ingestion/credentials/hdika`) — **το ισχυρότερο σήμα
  πρόθεσης**: κάποιος που έβαλε τους κωδικούς του δοκιμάζει στα σοβαρά.
- Αιτήματα δοκιμής/παρουσίασης από τα pop-up → `announcement_requests`.

**ΔΕΝ βλέπω σήμερα (και δεν θα το προσποιηθώ):**
- Προβολές σελίδων / GET. Το audit καταγράφει μόνο μεταβολές. Άρα «είδε το Dashboard» ή
  «επισκέφθηκε τις τιμές» **δεν είναι μετρήσιμα** χωρίς νέα καταγραφή.
- Ανοίγματα/κλικ email. Ο `mailer` δεν έχει pixel ούτε ανακατεύθυνση.
- Απαντήσεις σε email, bounces. Δεν διαβάζουμε γραμματοκιβώτιο.

**Πώς το λύνουμε τίμια:**
1. Το σκορ δραστηριότητας **Φάσης 1** χτίζεται μόνο σε ό,τι υπάρχει. Κάθε KPI που δεν
   υπολογίζεται δείχνει **«δεν μετριέται ακόμη»**, ποτέ μηδέν και ποτέ επινοημένο νούμερο.
2. Στη Φάση 2 προστίθενται ανοίγματα/κλικ με **δικά μας** tracking pixel & redirect, όπως
   ήδη υπάρχουν για τους ασθενείς (`/o/{cid}/{key}.gif`, `/c/{cid}/{key}`).
3. Οι προβολές σελίδων **δεν** μπαίνουν στη Φάση 1. Αν χρειαστούν, είναι ξεχωριστή απόφαση
   (καταγράφω δεδομένα χρήσης του πελάτη μου — θέλει τεκμηρίωση, όχι σιωπηλή προσθήκη).

---

## 3. Η προτεινόμενη αρχιτεκτονική

```
                       ┌──────────────────────────────────────┐
                       │  lead_projection  (μία φορά/ώρα)     │
                       │  tenants + subscriptions + users +   │
                       │  audit_logs + pending_registrations  │
                       │  + trial_leads  →  ΕΝΑ ενιαίο lead   │
                       └───────────────┬──────────────────────┘
                                       ▼
   ┌───────────────┐   ┌──────────────────────┐   ┌────────────────────┐
   │ lead_events   │◀──│       leads          │──▶│  lead_score        │
   │ (χρονολόγιο)  │   │  status, stage, tags │   │  σήματα + βαθμός   │
   └───────────────┘   └───────┬──────────────┘   └────────────────────┘
                               │
        ┌──────────────────────┼───────────────────────┐
        ▼                      ▼                       ▼
  ┌───────────┐        ┌──────────────┐        ┌───────────────┐
  │ lead_tasks│        │ lead_segments│        │ lead_campaigns│
  │ επόμενη   │        │ δυναμικοί    │        │ + recipients  │
  │ ενέργεια  │        │ κανόνες      │        │ + events      │
  └───────────┘        └──────────────┘        └───────┬───────┘
                                                       ▼
                                             ┌───────────────────┐
                                             │ platform_offers   │
                                             │ + coupons (v2)    │
                                             └───────────────────┘
```

### 3.1 Η κεντρική επιλογή: **προβολή (projection), όχι αντίγραφο**

Ο πειρασμός είναι να φτιάξω πίνακα `leads` και να αντιγράψω μέσα ονόματα, emails και
ημερομηνίες. Τότε την επόμενη μέρα ο πίνακας λέει άλλα από τη συνδρομή.

Αντ' αυτού: η `leads` κρατά **μόνο ό,τι δεν υπάρχει αλλού** — κατάσταση πωλήσεων, ετικέτες,
ανάθεση, σκορ, επόμενη ενέργεια. Όλα τα υπόλοιπα (όνομα, επαφή, ημερομηνίες trial, χρήση)
υπολογίζονται από την πηγή σε κάθε ανάγνωση ή ανανεώνονται από την ωριαία εργασία.

**Το κλειδί ταυτότητας** (`lead_key`) είναι σταθερό σε όλη τη ζωή: ΑΦΜ → αλλιώς email →
αλλιώς `tid:<tenant_id>`. Έτσι ένα φαρμακείο που δοκίμασε, διαγράφηκε και ξαναγράφτηκε είναι
**ο ίδιος lead** με ενιαίο ιστορικό. Οι σημερινές 6 εγγραφές `trial_leads` έχουν ήδη
ακριβώς αυτό το `_id` — **μεταφέρονται χωρίς μετατροπή**.

### 3.2 Πηγές ενός lead

| Πηγή | Πότε γίνεται lead | Σήμερα φαίνεται; |
|---|---|---|
| Ενεργή δοκιμαστική | με την έναρξη | ❌ |
| Δοκιμαστική που λήγει | αυτόματα | ❌ |
| Δοκιμαστική που έληξε, λογαριασμός ζωντανός | αυτόματα | ❌ (**5 σήμερα**) |
| Διαγραμμένη δοκιμαστική | `purge_expired_trials` | ✅ (6) |
| Ημιτελής εγγραφή | μετά από 24 ώρες | ❌ (**15 σήμερα**) |
| Αίτημα δοκιμής/παρουσίασης από pop-up | άμεσα | ❌ |
| Ληγμένη **πληρωμένη** συνδρομή (win-back) | μετά τη λήξη | ❌ |

### 3.3 Τι επαναχρησιμοποιείται — ρητά

| Υπάρχον | Χρήση στο Lead Engine | Αλλαγή; |
|---|---|---|
| `trial_leads` collection | **γίνεται** `leads` (ίδιο `_id`) | επέκταση πεδίων |
| `trial_leads.afm_had_trial` | μένει όπως είναι — κρίσιμο για μπλοκ επανα-trial | καμία |
| `billing_service.effective_status` | **η μόνη** πηγή κατάστασης συνδρομής | καμία |
| `audit_logs` | πηγή σημάτων δραστηριότητας | +1 index |
| `mailer.py` | ο αποστολέας κάθε lead email | καμία |
| `email_template.render_newsletter` | βάση των προτύπων | επέκταση |
| `feedback_service` coupons | **γίνεται** γενικός μηχανισμός προσφορών | επέκταση |
| `enforce_section` | ο έλεγχος πρόσβασης | +σύνδεση leads endpoints |
| `campaign_engine` (ασθενείς) | **δεν** αγγίζεται | καμία |
| `announcements` requests | γίνονται γεγονότα lead | καμία |
| Celery beat | νέες ωριαίες/ημερήσιες εργασίες | +4 |

### 3.4 Νέα στοιχεία

| Αρχείο | Ρόλος |
|---|---|
| `services/leads/projection.py` | χτίζει/ανανεώνει leads από τις πηγές |
| `services/leads/scoring.py` | σήματα → βαθμός, **ρυθμιζόμενα βάρη** |
| `services/leads/segments.py` | δυναμικοί κανόνες AND/OR πάνω σε πεδία lead |
| `services/leads/timeline.py` | ενιαίο χρονολόγιο (`lead_events`) |
| `services/leads/actions.py` | σημειώσεις, εργασίες, ετικέτες, ανάθεση |
| `services/leads/next_action.py` | κανόνες «τι κάνω τώρα» |
| `services/leads/campaigns.py` | καμπάνιες πλατφόρμας (Φάση 2) |
| `services/leads/offers.py` | προσφορές & κουπόνια (Φάση 3) |
| `services/leads/journeys.py` | αυτόματες διαδρομές (Φάση 4) |
| `workers/leads.py` | ωριαία προβολή, ημερήσια σκορ, αποστολές |
| `api/v1/routers/admin_leads.py` | ξεχωριστός router (ο `admin.py` είναι 3.700+ γραμμές) |
| `app/admin/leads/` | νέα οθόνη «Leads & Conversions» + καρτέλα lead |

---

## 4. Γεγονότα (events) χωρίς event bus

Δεν υπάρχει bus και **δεν θα φτιάξω** (over-engineering για 12 φαρμακεία). Αντ' αυτού:
μία συνάρτηση `timeline.record(lead_key, kind, payload)` που γράφει στο `lead_events`.
Είναι το ίδιο συμβόλαιο με ένα bus (ονόματα γεγονότων, φορτίο, χρονική σειρά) — χωρίς
υποδομή. Αν αύριο μπει bus, οι καλούντες δεν αλλάζουν.

Ονόματα γεγονότων (σταθερά, όχι ελεύθερο κείμενο):
`trial.started` `trial.expiring` `trial.expired` `trial.purged` `lead.created`
`lead.status_changed` `lead.activity` `lead.returned` `lead.note` `lead.task_created`
`lead.task_done` `lead.tagged` `lead.assigned` `email.sent` `email.opened` `email.clicked`
`offer.sent` `offer.opened` `offer.redeemed` `lead.converted` `lead.lost`
`lead.unsubscribed` `lead.suppressed`

**Πηγές:** άμεσα από τον κώδικα εκεί που συμβαίνει (αποστολή, αλλαγή status, σημείωση) και
από την ωριαία προβολή για όσα προκύπτουν από σύγκριση (πέρασε η λήξη, ξαναμπήκε μετά από
αδράνεια).

---

## 5. Ασφάλεια, πολυ-μισθωτικότητα, ιδιωτικότητα

1. **Το Lead Engine είναι αποκλειστικά platform-level.** Κανένα endpoint στον tenant router.
   Κάθε διαδρομή περνά από `enforce_section("leads")` — και διορθώνονται και τα υπάρχοντα.
2. **Κανένα δεδομένο ασθενή δεν αγγίζεται.** Το σκορ χτίζεται σε **αριθμούς ενεργειών** από
   το `audit_logs` (`action` + `tenant_id` + ώρα), ποτέ σε περιεχόμενο. Το `audit_logs.action`
   είναι URL· περιέχει σε λίγες περιπτώσεις `patient_id` στη διαδρομή → **η προβολή κρατά
   μόνο την κατηγορία ενέργειας, ποτέ την πλήρη διαδρομή**.
3. **Καμία εξατομίκευση με δεδομένα υγείας.** Οι επιτρεπτές μεταβλητές είναι κλειστή λίστα
   (§ RXVISION_LEAD_COMMUNICATION.md) — όνομα φαρμακείου, επαφή, ημερομηνίες, χρήση
   προϊόντος, κουπόνι. Οτιδήποτε άλλο απορρίπτεται στην απόδοση του προτύπου.
4. **Λειτουργικό ≠ προωθητικό.** Οι υπενθυμίσεις λήξης (`subscription_reminders`) είναι
   λειτουργικές: αφορούν σύμβαση σε ισχύ και **δεν** υπόκεινται σε διαγραφή ή όριο
   συχνότητας. Οι προσφορές είναι προωθητικές και υπόκεινται σε όλα.
5. **Νομικά σημεία που ΔΕΝ αποφασίζω μόνος μου** και υλοποιούνται ως ρύθμιση:
   - Αν η B2B επικοινωνία προς φαρμακείο που **δοκίμασε** το προϊόν απαιτεί ρητή
     συγκατάθεση ή στηρίζεται σε έννομο συμφέρον → ρύθμιση `require_optin` (προεπιλογή:
     **ναι, απαιτεί**, δηλαδή το αυστηρότερο).
   - Πόσο κρατάμε lead χωρίς επαφή → ρύθμιση `lead_retention_months` (προεπιλογή 24).
   - Και τα δύο καταγράφονται ως ανοιχτά στο τέλος αυτού του κειμένου.
6. **Audit trail:** κάθε ενέργεια πωλήσεων περνά από τον `AuditMiddleware` (είναι POST/PATCH)
   **και** γράφεται στο χρονολόγιο του lead. Διπλή καταγραφή σκόπιμα: η μία για ασφάλεια,
   η άλλη για τον πωλητή.

---

## 6. Φάσεις υλοποίησης

| Φάση | Περιεχόμενο | Κριτήριο «τελείωσε» |
|---|---|---|
| **1 — Νοημοσύνη** | προβολή, κύκλος ζωής, σκορ, τμήματα, χρονολόγιο, σημειώσεις, εργασίες, ετικέτες, πίνακας, «Σήμερα», επόμενη ενέργεια | τα 5 ληγμένα trial + οι 15 ημιτελείς εγγραφές εμφανίζονται με πραγματικό σκορ και πρόταση ενέργειας |
| **2 — Επικοινωνία** | καμπάνιες πλατφόρμας με ουρά, πρότυπα, ανοίγματα/κλικ, διαγραφή, όριο συχνότητας· **το newsletter απορροφάται** | καμία αποστολή μέσα σε HTTP request· κάθε email έχει σύνδεσμο διαγραφής |
| **3 — Προσφορές** | γενικό μοντέλο προσφοράς, κουπόνια v2, στόχευση μέσω τμημάτων, εξαργύρωση, απόδοση μετατροπής | μια προσφορά φτάνει σε τμήμα, εξαργυρώνεται και η μετατροπή αποδίδεται σε αυτήν |
| **4 — Αυτοματισμοί** | αφορμή → συνθήκη → αναμονή → ενέργεια, με τα ίδια φρένα | μια διαδρομή τρέχει μόνη της με πλήρες χρονολόγιο |
| **5 — AI** | προτάσεις μηνύματος & επόμενης ενέργειας | **ποτέ** αποστολή χωρίς ανθρώπινη έγκριση |

---

## 7. Ανοιχτά σημεία που θέλουν απόφαση ιδιοκτήτη

1. **Νομική βάση B2B προσφορών** (§5.5) — ρυθμιζόμενο, ξεκινά στο αυστηρό.
2. **Καταγραφή προβολών σελίδων** για ακριβέστερο σκορ — δεν την πρόσθεσα μόνος μου.
3. **ΓΙΑΝΝΑΚΑΚΗ & Hivepharmacy**: `status=active` σε δωρεάν `trial` πλάνο. Το Lead Engine
   θα τα εμφανίσει ως leads· αν είναι πελάτες χάριτος, θέλουν σήμανση `complimentary`.
4. **Ποιος αναλαμβάνει τα leads** — η ανάθεση δείχνει σε `platform_admins` (5 λογαριασμοί).
