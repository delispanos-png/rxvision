# runs/

Timestamped logs from `scripts/ai/run-task.sh` land here (`<ts>-<label>.log`).

Local scratch only — gitignored, never committed. Distil anything that matters into
the journal or `project-state.md`; treat these files as disposable.
