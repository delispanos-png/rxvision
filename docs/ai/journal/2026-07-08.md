# Session — 2026-07-08

Branch: `feature/loyalty-portal-build`. Commit `03ba7ae` pushed. Deployed live throughout
(final BUILD_ID `fh0Iy1C7FUk0p8RkRuaAj`, app nodes 10.0.0.5 + 10.0.0.6).

## Shipped

**Customer messaging (central Apifon):** per-message log (`sent_messages`) + delivery-status
DLR webhook (`/communications/apifon-dlr`) with auto wallet-refund on non-delivery; tenant
message-history table; cost-before-send + insufficient-balance guard in campaign UI;
low-balance email alert. Requirements doc `docs/apifon-api-requirements.md`.

**Subscription plan changes:** rewrote «Modules/Πλάνο» tab into a plan view (current plan +
selectable packages + add-ons preview). Free/trial (€0) not selectable. Upgrade = card
(Revolut) / Alpha Bank / bank-transfer→admin-approval request; downgrade scheduled to period
end (daily beat `apply-scheduled-changes`). Card applied via Revolut webhook. Service:
`plan_change_service.py`. Admin «Αιτήματα αναβάθμισης» page.

**Payments:** admin enable/disable payment methods (`payment_methods.py`); tenant upgrade +
registration adapt to enabled ones. **Alpha Bank** (Alpha e-Commerce redirect) scaffold +
`docs/alphabank-api-requirements.md` (needs live creds + gateway/digest confirmation).
**Παραστατικά** (receipts) on Χρέωση tab (`receipts.py` → `payments` collection + wallet topups).

**Admin IA overhaul:** whole adminpanel regrouped into 6 collapsible circuits + Πίνακας. Split
old «Πληρωμές & ΑΑΔΕ» → `/admin/payments` (methods+Revolut+Alpha+bank), `/admin/aade`,
`/admin/ai-providers`. Removed `/admin/integrations` page.

**Misc:** app version **v1.1.1** in sidebar footer (`frontend/src/lib/version.ts`) — bump on
notable changes. Optical Audit marked **beta**. Removed dead `schemas/subscriptions.py` +
`set_checkout_pending`.

## Open / next
- ⚠️ Admin must fill **bank IBAN** (Τρόποι πληρωμής) for bank-transfer upgrades to work.
- ⚠️ **Apifon** must enable DLR callbacks + confirm payload/signature (doc ready to send).
- ⚠️ **Alpha Bank** live activation needs merchant creds + gateway URL/digest confirmation.
- Optional: adapt the public `/register` wizard UI to show only enabled payment methods
  (data already exposed via `/onboarding/payment-methods`).
