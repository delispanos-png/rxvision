# 2026-09-24 — RxVision Connect (v1.51.0)

Ολόκληρη η ημέρα σε ένα κύκλωμα: **δίκτυα συνεργασίας φαρμακείων**. Παραδόθηκε προδιαγραφή 41
ενοτήτων με ρητή εντολή «audit πρώτα, κώδικας μετά».

## 1. Το audit βρήκε αυτό που ακύρωνε τη μισή προδιαγραφή

Τα §9–§14 (ποσοστό αποθέματος, safety stock, καθολικό όριο, δυναμικός υπολογισμός) στηρίζονται
σε αξιόπιστο απόθεμα. Μέτρηση στην παραγωγή:

```
pharmacy_products:        41.127 είδη
με stock_qty > 0:             13 είδη   ← σε ΟΛΟ το πελατολόγιο
pharmacy_stock_movements:      3 κινήσεις
```

Τα φαρμακεία κρατούν απόθεμα στο εμπορικό τους πρόγραμμα· ο `catalog_seed` φορτώνει σκόπιμα με
απόθεμα μηδέν. Αν το κύκλωμα υπολόγιζε μόνο από απόθεμα, θα έδειχνε «κανείς δεν μπορεί να
καλύψει» σε **15/15 φαρμακεία** από την πρώτη μέρα — η ίδια παγίδα με το `daily_coach`.

**Απόφαση ιδιοκτήτη:** ό,τι ξέρουμε από το ράφι απαντά **αυτόματα**· για τα υπόλοιπα **ρωτάμε
τον φαρμακοποιό** και απαντά αποδοχή/άρνηση. Στον κώδικα: `available_qty()` επιστρέφει `None`
(≠ 0) για άγνωστο απόθεμα.

## 2. Άλλες αποφάσεις ιδιοκτήτη

- **Ξεχωριστές ομάδες** από τη χθεσινή συνομιλία — χωρίς να εμποδίζεται φαρμακείο να είναι και
  στα δύο. Για να μην αποκλίνουν, ο κοινός μηχανισμός (ΑΦΜ → φαρμακείο, φρουρός εισόδου) βγήκε
  σε `services/pharmacy_directory.py` και τον χρησιμοποιούν και τα δύο κυκλώματα.
- **Όλοι πληρώνουν Connect**: η πρόσκληση απορρίπτεται αν ο παραλήπτης δεν έχει το module.
- **30 €/μήνα · 300 €/έτος**.

## 3. Τι χτίστηκε

`repositories/connect.py` (όλοι οι κανόνες, 740 γρ.) · `services/connect_availability.py`
(ΕΝΑΣ υπολογισμός) · `services/pharmacy_directory.py` · `api/v1/routers/connect.py` (26
endpoints) · `workers/connect.py` (λήξη κρατήσεων, beat/5΄) · μία σελίδα με 6 καρτέλες ·
εικονίδιο Topbar με εκκρεμότητες.

**Τρεις διακριτές στιγμές** — εμφάνιση (τίποτα δεν δεσμεύεται) → αποδοχή (κράτηση με λήξη) →
«Παρέδωσα» (απόθεμα + ledger **και στα δύο** φαρμακεία). Το υπόλοιπο είναι **παράγωγο** από τις
κινήσεις, ποτέ αποθηκευμένος μετρητής.

## 4. Τεκμηριωμένη εξαίρεση στον ΕΝΑ κανόνα

Το Connect είναι εξ ορισμού διατενάντ, άρα δεν περνά από `BaseRepository` (ίδιο μοτίβο με το
`pharmacy_chat`). Η ορατότητα ορίζεται μία φορά (`_my_groups` → `_visible_requests_q`/`_mine_q`)
και κάθε ανάγνωση περνά από εκεί. Το `tests/test_connect_flows.py` το φυλάει.

## 5. Δύο σφάλματα που βρήκαν τα ίδια τα σενάρια

1. **Νέα προσφορά έγραφε πάνω σε ήδη αποδεκτή** (`update_one` με `upsert`) → θα ξεκλείδωνε
   ποσότητα που ήταν ήδη δεσμευμένη σε κίνηση. Φρουρός `already_accepted`.
2. **Η παρτίδα γραφόταν μετά** την ενημέρωση αποθέματος, οπότε δεν έφτανε ποτέ στο ledger.
   Αντιστράφηκε η σειρά.

## 6. Επαλήθευση

**34 έλεγχοι, όλα τα 20 σενάρια του §36**, σε πραγματική βάση με δοκιμαστικά φαρμακεία
(`T-ZZTEST-*`, καθαρισμός πριν και μετά — καμία εγγραφή στον πίνακα `tenants`). Σώθηκαν ως
`tests/test_connect_availability.py` (καθαρό, τρέχει πάντα) και `tests/test_connect_flows.py`
(παρακάμπτεται χωρίς Mongo).

## Ανοιχτά / V2

Έξυπνο ταίριασμα, **γέφυρα δικτύων** (§23 — συγκρούεται με το §3, λύση: ρητή ανθρώπινη
προώθηση από τον ενδιάμεσο), λειτουργία έκτακτης ανάγκης, δείκτες αξιοπιστίας.
Επίσης: ειδοποιήσεις μέσω email/SMS (σήμερα υπάρχει μόνο η ένδειξη στο Topbar).

---

## Απόγευμα — «Λέει την αλήθεια το RxVision;»

Ξεκίνησε από αναφορά φαρμακοποιού για τη συνταγή `2606152973523` (Tegretol) και κατέληξε σε
τρεις εκδόσεις και έναν πλήρη έλεγχο των δεδομένων ΗΔΥΚΑ.

### v1.55.0 — Μερική εκτέλεση (`executed_qty`)
Η ΗΔΥΚΑ δίνει **«Υπόλοιπο» ανά γραμμή** (CDA 1.4.19) = τεμάχια που ΔΕΝ δόθηκαν. Το μοντέλο μας
κρατούσε μόνο ναι/όχι, οπότε «2 κουτιά, δόθηκε 1» καταγραφόταν ως **ολικά ανεκτέλεστο**.
- **17.093 γραμμές** σε **όλα τα 13 φαρμακεία** · **22.435 τεμάχια** δεν μετρούσαν ·
  **232.885 €** ψεύτικης απώλειας αφαιρέθηκαν.
- Το `details.outstanding` **αποθηκευόταν ήδη** → backfill **τοπικά**, χωρίς επανα-άντληση
  (`scripts/backfill_executed_qty.py`, 1.055.828 γραμμές).
- Διορθώθηκαν ΚΑΙ οι σιωπηλές συνέπειες: `patient_intelligence` / `pharmacat` /
  `patient_portal` έκρυβαν το μισο-δοσμένο φάρμακο → **έχανε ο έλεγχος αλληλεπιδράσεων**.

### v1.56.0 — «Ανοιχτή» ≠ «ανακτήσιμη» (`services/recoverable.py`)
Χρειάζονται ΔΥΟ όροι: ο ΛΟΓΟΣ (case 0) **ΚΑΙ ΤΟ ΡΟΛΟΪ** (προθεσμία). Ελέγχαμε μόνο τον λόγο:
από **26.778** ανεκτέλεστες μόνο **1.561** ήταν όντως ανακτήσιμες (25.217 είχαν λήξει, οι
**14.586 πάνω από έναν χρόνο**). Μαζί, δύο bugs εμφάνισης: η λίστα δεν έστελνε καθόλου τον λόγο
(κάθε μερική έγραφε «έκλεισε»), και το πλαίσιο χρωματιζόταν από το `is_executed` (η μισο-δοσμένη
έγραφε «Δόθηκε 1 από 2» μέσα σε γκρι κουτί).

### v1.57.0/1 — Νομική αλήθεια στα μηνύματα
Ο Σύμβουλος έλεγε «{money} που πιθανότατα θα καταλήξουν σε **άλλο φαρμακείο**». **Ψευδές.**
Ελληνική νομοθεσία: το υπόλοιπο **ΜΙΑΣ εκτέλεσης** κλειδώνει στο φαρμακείο που την ξεκίνησε.
Οι **επόμενες** εκτελέσεις (2,3,4…) είναι **ελεύθερες**. Μπήκε σε Σύμβουλο, advisor, καρτέλα
ασθενή, tooltip λίστας και — κρίσιμα — στο **SYSTEM prompt του Copilot** (γράφει ελεύθερο κείμενο
και αλλιώς *εφευρίσκει* το «πήγε αλλού»).

### Έλεγχος δεδομένων ΗΔΥΚΑ (25 πραγματικές συνταγές, 8 φαρμακεία, ζωντανά)
- **11 από 14 πεδία ταυτίζονται απόλυτα.** Οι 3 διαφορές εξηγούνται πλήρως (παράθυρο
  backfill→deploy· σωρευτικό CDA σε πολλαπλές εκτελέσεις). **Μηδέν πραγματικά λάθη.**
- **63 από 132 αναγνωριστικά δεν τα διαβάζουμε** — τα περισσότερα θόρυβος· δεν υπάρχει επίσημο
  λεξικό πεδίων, δουλεύουμε με αντίστροφη μηχανική.
- **ΕΥΡΗΜΑ:** `details.exec_count` + `active_executions` (= πόσες εκτελέσεις έχει η ΗΔΥΚΑ)
  αποθηκεύονται και **δεν χρησιμοποιούνται πουθενά**. Ως έλεγχος πληρότητας δίνουν
  **363 συνταγές / 521 εκτελέσεις που η ΗΔΥΚΑ έχει κι εμείς όχι**, σε 10/13 φαρμακεία —
  και συμφωνεί με ανεξάρτητη ανίχνευση κενών (356).

### Απόθεμα — επιβεβαιωμένα σιωπηλό
13 είδη με απόθεμα σε ΟΛΑ τα φαρμακεία. Ελέγχθηκε όλη η επιφάνεια: κανένα ψευδές μήνυμα δεν
βγαίνει. Κάθε δυνατότητα αποθέματος **πάει για αργότερα** (απόφαση ιδιοκτήτη).
